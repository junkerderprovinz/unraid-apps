<?xml version="1.0"?>
<Container version="2">
  <Name>Stellarium</Name>
  <Beta>false</Beta>
  <Repository>junkerderprovinz/stellarium:latest</Repository>
  <Registry>https://hub.docker.com/r/junkerderprovinz/stellarium</Registry>
  <Network>bridge</Network>
  <Shell>bash</Shell>
  <Privileged>false</Privileged>
  <ReadMe>https://github.com/junkerderprovinz/stellarium/blob/main/README.md</ReadMe>
  <Support>https://forums.unraid.net/topic/198811-support-junkerderprovinz-unraid-apps/</Support>
  <Project>https://stellarium.org/</Project>
  <Overview>
Stellarium — the free, open-source desktop planetarium — on a Selkies web desktop. Explore a photorealistic real-time sky in your browser: no VNC client, no local install. There is no maintained browser-desktop build of the actual Stellarium application ("Stellarium Web" is a separate, much lighter reimplementation), so this fills a genuine gap: a modern Selkies (WebRTC) build of the real thing for amd64 and arm64.&#xD;
&#xD;
What you get:&#xD;
• The full Stellarium desktop app, streamed over WebRTC (Selkies) so panning the sky, zooming into a nebula and scrubbing through time stay smooth — the continuously-rendered workload where the old noVNC containers feel laggy.&#xD;
• Installed from Debian trixie's stellarium package, so it tracks Debian security updates.&#xD;
• Software-GL (Mesa llvmpipe) fallback so the sky renders even without a GPU; a passed-through GPU is used when present.&#xD;
• Your location, downloaded star catalogues, landscapes, plugins and screenshots persist under /config.&#xD;
&#xD;
Notes:&#xD;
• WebUI opens via HTTPS on port 3001 (self-signed cert — accept it once). The Selkies web client requires a secure connection, so open the WebUI directly ONLY over HTTPS (3001). Port 3000 (HTTP) is for a reverse proxy that terminates TLS in front of the container — opening it directly shows a "requires a secure connection (HTTPS)" error.&#xD;
• PUID/PGID default to 99/100 (nobody/users on Unraid).&#xD;
• Auth: leave "WebUI Username" and "Password" EMPTY to run without login. Set both to enable HTTP-basic auth on the WebUI. Recommended whenever the container is reachable beyond your LAN.&#xD;
• GPU: a passed-through GPU is used when present; otherwise it falls back to software rendering.&#xD;
&#xD;
Source on GitHub: https://github.com/junkerderprovinz/stellarium&#xD;
  </Overview>
  <Category>Tools: Productivity:</Category>
  <WebUI>https://[IP]:[PORT:3001]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/junkerderprovinz/unraid-apps/main/stellarium/stellarium.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/junkerderprovinz/unraid-apps/main/stellarium/icon.png</Icon>
  <ExtraParams>--restart=unless-stopped --shm-size=1gb</ExtraParams>

  <!-- ========================================================
       PORTS
       ======================================================== -->
  <Config Name="WebUI Port (HTTPS / Selkies)" Target="3001" Default="3001" Mode="tcp"
          Description="HTTPS port of the Selkies WebUI (self-signed cert). This is the default WebUI — HTTPS is required for clipboard support in the browser. Accept the self-signed certificate once."
          Type="Port" Display="always" Required="true" Mask="false">3001</Config>

  <Config Name="WebUI Port (HTTP / Selkies)" Target="3000" Default="3000" Mode="tcp"
          Description="HTTP port of the Selkies WebUI. For a reverse proxy that terminates TLS in front of the container — NOT for direct browser access. The Selkies web client requires a secure context, so opening http://IP:3000 directly shows a 'requires a secure connection (HTTPS)' error. For direct access use the HTTPS port 3001."
          Type="Port" Display="always" Required="false" Mask="false">3000</Config>

  <!-- ========================================================
       PATHS
       ======================================================== -->
  <Config Name="AppData (/config)" Target="/config" Default="/mnt/user/appdata/stellarium" Mode="rw"
          Description="Persistent Stellarium data: your chosen location, downloaded star catalogues and DSO textures, landscapes, enabled plugins, screenshots and preferences (stored under /config/.stellarium)."
          Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/stellarium</Config>

  <!-- ========================================================
       SCREEN SIZE (drives the container's memory use)
       ======================================================== -->
  <Config Name="Screen Size (MAX_RES)" Target="MAX_RES"
          Default="15360x8640 (full, ~565 MB)|7680x4320 (8K, ~185 MB)|5120x2880 (5K, ~115 MB)|3840x2160 (4K, ~90 MB)|3440x1440 (ultrawide, ~80 MB)|2560x1440 (1440p, ~75 MB)|1920x1080 (1080p, ~65 MB)" Mode=""
          Description="Largest virtual screen the container can serve, and the single biggest factor in its memory use: the X server reserves the whole framebuffer up front, about 4 bytes per pixel, no matter how big your browser window is. The full 15360x8640 is the default and keeps every resolution available; picking a smaller one trades that headroom for RAM (the bracketed figures are the measured X server memory for each; the application itself comes on top). Pick a size at least as big as your largest browser window in physical pixels: a 1600x1000 window on a laptop set to 200 % needs 3200x2000. A bigger window does not get a bigger desktop; it shows the desktop in its top-left corner and the rest stays black. To enter a size that is not listed, use the field below instead."
          Type="Variable" Display="always" Required="false" Mask="false">15360x8640 (full, ~565 MB)</Config>

  <Config Name="Screen Size, custom (MAX_RES_CUSTOM)" Target="MAX_RES_CUSTOM"
          Default="" Mode=""
          Description="Optional. Your own screen size as WIDTHxHEIGHT, for example 3440x1440 or 6016x3384. When this field has a value it overrides the dropdown above, so you are not limited to the presets. Leave it empty to use the dropdown. A value that is not a WIDTHxHEIGHT pair is ignored with a note in the container log, so a typo cannot stop the container from starting."
          Type="Variable" Display="always" Required="false" Mask="false"/>

  <!-- ========================================================
       WEBUI AUTH (SELKIES)
       ======================================================== -->
  <Config Name="WebUI Username" Target="CUSTOM_USER" Default="" Mode=""
          Description="Optional. Username for HTTP-basic-auth on the WebUI. Leave EMPTY together with the password for no login. Set both fields to enable login (recommended outside trusted LANs)."
          Type="Variable" Display="always" Required="false" Mask="false"/>

  <Config Name="WebUI Password" Target="PASSWORD" Default="" Mode=""
          Description="Optional. Password for HTTP-basic-auth on the WebUI. Leave EMPTY together with the username for no login. STRONGLY recommended whenever the container is reachable from outside your LAN."
          Type="Variable" Display="always" Required="false" Mask="true"/>

  <!-- ========================================================
       ADVANCED ENV VARIABLES
       ======================================================== -->
  <Config Name="Timezone (TZ)" Target="TZ" Default="Europe/Vienna" Mode=""
          Description="Container timezone (affects log timestamps and file mtimes). Use this instead of a /etc/localtime bind-mount — the LSIO base image manages /etc/localtime internally via TZ."
          Type="Variable" Display="always" Required="false" Mask="false">Europe/Vienna</Config>

  <Config Name="User ID (PUID)" Target="PUID" Default="99" Mode=""
          Description="User-ID Stellarium runs as. Default 99 (nobody on Unraid)."
          Type="Variable" Display="advanced" Required="true" Mask="false">99</Config>

  <Config Name="Group ID (PGID)" Target="PGID" Default="100" Mode=""
          Description="Group-ID. Default 100 (users on Unraid)."
          Type="Variable" Display="advanced" Required="true" Mask="false">100</Config>

  <Config Name="UMASK" Target="UMASK" Default="000|022|002|077" Mode=""
          Description="Permission mask for newly created files. The default 000 lets every container user read and write them, which is what you want when other containers touch the same shares. Set 022 or 077 for stricter permissions."
          Type="Variable" Display="advanced" Required="false" Mask="false">000</Config>

</Container>
