<?xml version="1.0"?>
<Container version="2">
  <Name>Matrix</Name>
  <Repository>junkerderprovinz/matrix:latest</Repository>
  <Registry>https://hub.docker.com/r/junkerderprovinz/matrix</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <ReadMe>https://github.com/junkerderprovinz/matrix/blob/main/README.md</ReadMe>
  <Support>https://forums.unraid.net/topic/198818-support-junkerderprovinz-matrix-aio/</Support>
  <Project>https://matrix.org</Project>
  <Overview>
    All-in-one Matrix homeserver for Unraid: Synapse + coturn + Element Web (/element/) + Synapse-Admin (/admin/).
    Plug-and-play — just fill in SERVER_NAME and Postgres connection, everything else is generated automatically.

    PREREQUISITES (before starting the container):

    1) PostgreSQL database — connect to your Postgres container and run:
       CREATE USER matrix WITH PASSWORD 'yourpass';
       CREATE DATABASE matrix ENCODING 'UTF8' LC_COLLATE='C' LC_CTYPE='C' TEMPLATE template0 OWNER matrix;
       (UTF8 + LC_COLLATE='C' is mandatory — Synapse will refuse to start otherwise.)

    2) Reverse proxy (Nginx Proxy Manager) for matrix.yourdomain.tld &#x2192; Unraid-IP:8008.
       In the proxy host &#x2192; Advanced tab, add:
         client_max_body_size 100m;
         proxy_read_timeout 600s;
       (Without these, media uploads fail and long-running requests time out.)

    3) For federation: two extra Custom Locations on the SAME proxy host for
       /.well-known/matrix/server and /.well-known/matrix/client &#x2192; Unraid-IP:8080.
       Full step-by-step guide (NPM screenshots, well-known, first user):
       https://github.com/junkerderprovinz/matrix#readme
  </Overview>
  <Category>Network:Messenger Network:Other Productivity:</Category>
  <WebUI>http://[IP]:[PORT:8080]/element/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/junkerderprovinz/unraid-docker-templates/main/matrix/matrix.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/junkerderprovinz/unraid-docker-templates/main/matrix/icon.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires>An external PostgreSQL database (UTF8, LC_COLLATE=C) and a reverse proxy (e.g. Nginx Proxy Manager) with large-upload and long-timeout settings. See the documentation for the exact setup: https://github.com/junkerderprovinz/matrix#readme</Requires>

  <!-- =========================================================
       PORTS
       ========================================================= -->

  <Config Name="Matrix HTTP API" Target="8008" Default="8008" Mode="tcp"
          Description="Synapse HTTP API. Expose via reverse proxy with HTTPS — never directly to the internet."
          Type="Port" Display="always" Required="true" Mask="false">8008</Config>

  <Config Name="Element Web + Synapse-Admin" Target="8080" Default="8080" Mode="tcp"
          Description="Serves Element Web (/element/), Synapse-Admin (/admin/) and /.well-known/matrix/* endpoints."
          Type="Port" Display="always" Required="true" Mask="false">8080</Config>

  <Config Name="TURN TCP" Target="3478" Default="3478" Mode="tcp"
          Description="TURN/STUN over TCP for voice/video calls. Open this port in your router/firewall."
          Type="Port" Display="always" Required="true" Mask="false">3478</Config>

  <Config Name="TURN UDP" Target="3478" Default="3478" Mode="udp"
          Description="TURN/STUN over UDP for voice/video calls. Open this port in your router/firewall."
          Type="Port" Display="always" Required="true" Mask="false">3478</Config>

  <Config Name="TURN TLS TCP" Target="5349" Default="5349" Mode="tcp"
          Description="TURN over TLS (TCP). Optional — only if TLS certs are mounted."
          Type="Port" Display="advanced-hide" Required="false" Mask="false">5349</Config>

  <Config Name="TURN TLS UDP" Target="5349" Default="5349" Mode="udp"
          Description="TURN over TLS (UDP). Optional — only if TLS certs are mounted."
          Type="Port" Display="advanced-hide" Required="false" Mask="false">5349</Config>

  <Config Name="Prometheus Metrics" Target="9090" Default="9090" Mode="tcp"
          Description="Prometheus metrics at /_synapse/metrics. Bind to a private network only."
          Type="Port" Display="advanced" Required="false" Mask="false">9090</Config>

  <!-- =========================================================
       VOLUME
       ========================================================= -->

  <Config Name="Data Directory" Target="/data" Default="/mnt/user/appdata/matrix/"
          Description="Persistent data: homeserver.yaml, media, signing keys. Never delete — destroys keys and breaks federation."
          Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/matrix/</Config>

  <Config Name="TURN-TLS Certs" Target="/data/certs" Default="" Mode="ro"
          Description="Optional. Mount a folder containing fullchain.pem + privkey.pem to enable TURN-TLS on 5349."
          Type="Path" Display="advanced" Required="false" Mask="false"/>

  <!-- =========================================================
       REQUIRED ENVIRONMENT VARIABLES
       ========================================================= -->

  <Config Name="Matrix Server Domain" Target="SERVER_NAME" Default=""
          Description="Your Matrix domain, e.g. matrix.yourdomain.tld. Used in Matrix IDs (@user:matrix.yourdomain.tld). Cannot be changed later without wiping the DB."
          Type="Variable" Display="always" Required="true" Mask="false">matrix.yourdomain.tld</Config>

  <Config Name="Anonymous Usage Reporting" Target="REPORT_STATS" Default="no"
          Description="Send anonymous usage stats to Matrix.org. 'yes' or 'no'. Recommended: no."
          Type="Variable" Display="always" Required="true" Mask="false">no</Config>

  <Config Name="Enable Federation" Target="ENABLE_FEDERATION" Default="true"
          Description="true = chat with the public Matrix network (requires /.well-known/matrix/* on your proxy, see README). false = private island."
          Type="Variable" Display="always" Required="true" Mask="false">true</Config>

  <Config Name="PostgreSQL Host" Target="POSTGRES_HOST" Default="192.168.x.x"
          Description="IP of your PostgreSQL server (e.g. Unraid host IP). IP+port is more reliable than container names."
          Type="Variable" Display="always" Required="true" Mask="false">192.168.x.x</Config>

  <Config Name="PostgreSQL Port" Target="POSTGRES_PORT" Default="5432"
          Description="PostgreSQL port. Default 5432."
          Type="Variable" Display="always" Required="true" Mask="false">5432</Config>

  <Config Name="PostgreSQL Username" Target="POSTGRES_USER" Default="admin"
          Description="Postgres user for Synapse. Must already exist (see README — Postgres setup)."
          Type="Variable" Display="always" Required="true" Mask="false">admin</Config>

  <Config Name="PostgreSQL Password" Target="POSTGRES_PASSWORD" Default=""
          Description="Password for the Postgres user above."
          Type="Variable" Display="always" Required="true" Mask="true"/>

  <Config Name="PostgreSQL Database" Target="POSTGRES_DB" Default="matrix"
          Description="Synapse database name. Must be created with ENCODING UTF8, LC_COLLATE='C', LC_CTYPE='C' — see Overview."
          Type="Variable" Display="always" Required="true" Mask="false">matrix</Config>

  <!-- =========================================================
       OPTIONAL: AUTO-CREATE ADMIN USER ON FIRST BOOT
       Set both variables once to have an admin account created
       automatically on first boot. After the user has been created
       you can safely clear both fields. The same credentials are
       used to sign in to Element Web and Synapse-Admin.
       ========================================================= -->

  <Config Name="Matrix Admin Username (Element / Synapse-Admin login)" Target="ADMIN_USER" Default=""
          Description="Optional. Localpart of the admin user to auto-create on first boot (e.g. 'admin' → @admin:SERVER_NAME). Logs into Element Web + Synapse-Admin. Clear after creation."
          Type="Variable" Display="always" Required="false" Mask="false"/>

  <Config Name="Matrix Admin Password (Element / Synapse-Admin login)" Target="ADMIN_PASSWORD" Default=""
          Description="Optional. Password for the admin user above. Clear after first boot."
          Type="Variable" Display="always" Required="false" Mask="true"/>

  <!-- =========================================================
       ADVANCED ENVIRONMENT VARIABLES
       ========================================================= -->

  <Config Name="Timezone" Target="TZ" Default="Europe/Vienna"
          Description="Container timezone (affects log timestamps)."
          Type="Variable" Display="advanced" Required="false" Mask="false">Europe/Vienna</Config>

  <Config Name="User ID (PUID)" Target="PUID" Default="99"
          Description="UID Synapse runs as. Default 99 (nobody on Unraid)."
          Type="Variable" Display="advanced" Required="false" Mask="false">99</Config>

  <Config Name="Group ID (PGID)" Target="PGID" Default="100"
          Description="GID Synapse runs as. Default 100 (users on Unraid)."
          Type="Variable" Display="advanced" Required="false" Mask="false">100</Config>

</Container>
